workflows:
  qsuk-builder:
    name: QSUK APK Builder
    max_build_duration: 30
    environment:
      android: latest
      vars:
        MODULE_PATH: "app"
    scripts:
      - name: Сборка APK
        script: |
          cd $MODULE_PATH
          chmod +x gradlew
          ./gradlew clean assembleRelease
    artifacts:
      - app/build/outputs/apk/release/*.apk
